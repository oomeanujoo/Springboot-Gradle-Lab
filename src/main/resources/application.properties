server.port=8082
spring.application.name=microservice

#spring.datasource.url = jdbc:postgresql://localhost:5432/postgres
#spring.datasource.username=postgres
#spring.datasource.password=${DB_LOCAL_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver

# Never commit a real signing key. Set JWT_SECRET in the OS/environment.
app.jwt.secret=${JWT_SECRET:local-development-key-change-before-real-use-12345678901234567890}

#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# disable security for now
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Kafka
# spring.kafka.bootstrap-servers=localhost:9092
# spring.kafka.consumer.group-id=earth-group
# spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
# spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
# spring.kafka.consumer.properties.spring.json.trusted.packages=*
#
# spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
# spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
#
# Topics
# kafka.topic.request=blackhole-requests
# kafka.topic.response=blackhole-responses

# Disable Kafka listeners
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.listener.auto-startup=false
